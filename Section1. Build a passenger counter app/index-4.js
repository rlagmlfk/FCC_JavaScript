
// onclick

// initialize the count as 0
// listen for clicks on the increment button
// incremet the count variable when the button is clicked
// change the count-el in the HTML


function increment() {
    console.log("the button was clicked");
}
